name: avatar_better
description: "This is a complete avatar package that you can use for your profile and avatar."
version: 1.0.5
homepage: https://github.com/SwanFlutter/avatar_better.git
uses-material-design: true

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  zoom_hover_pinch_image: ^1.1.0
  picker_image_cropper: ^0.0.6
  flutter_saver: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:


