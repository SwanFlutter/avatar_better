name: avatar_better
description: "This is a complete avatar package that you can use for your profile and avatar."
version: 0.0.7
homepage: https://github.com/SwanFlutter/avatar_better.git
uses-material-design: This

platforms:
  android:
  ios:
  web:

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_cropper: ^7.1.0
  image_picker: ^1.0.5
  path: ^1.8.3
  image_downloader_web: ^2.0.4
  path_provider: ^2.1.3
  path_provider_foundation: ^2.4.0
  android_path_provider: ^0.3.0
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0


flutter:


